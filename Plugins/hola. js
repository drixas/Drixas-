export default {
  name: 'hola',
  description: 'El bot te saluda usando tu nombre (pushname)',
  async run(m, { conn }) {
    // Revisar si pushname está disponible, si no usar "amig@" como fallback
    const pushname = m.pushName || m.sender?.split('@')[0] || "amig@";
    const saludo = `👋 ¡Hola, ${pushname}! ¿Cómo estás?`;

    // Envía el saludo, citando el mensaje original del usuario
    await conn.sendMessage(m.chat, { text: saludo }, { quoted: m });
  }
}